# 工作流定义

AI Agent 读取本文件后按流程执行新闻分析和发布任务。

## 触发方式

当用户说以下内容时，**直接按以下流程执行**，不需要询问是否使用网络搜索：

| 用户说法 | 说明 |
|---------|------|
| "分析新闻" 或 "发日报" | 分析昨天7:00到今天7:00的新闻（默认时间范围） |
| "分析从昨天下午到今天的新闻" | 自定义时间范围，调整脚本参数 |
| "分析5月30号的新闻" | 指定日期，计算对应时间范围 |

## 完整工作流程

```
阶段1：生成日报
┌──────────────────────────────────────────────────────┐
│ 1. 检查 wechat-article-exporter 服务是否运行         │
│    → 访问 http://localhost:3200                      │
│    → 如未运行：启动服务                               │
│      cd wechat-article-exporter                      │
│      npx nuxt dev --port 3200                        │
│    → 注意：PowerShell 可能阻止 npx，使用完整路径：    │
│      & "C:\Program Files\nodejs\npx.cmd" nuxt dev --port 3200 │
├──────────────────────────────────────────────────────┤
│ 2. 检查登录状态                                      │
│    → 访问 http://localhost:3200                      │
│    → 如显示"invalid session"或需要登录：              │
│      告诉用户"需要扫码登录，请在浏览器中操作"         │
│      等待用户扫码完成后再继续                         │
├──────────────────────────────────────────────────────┤
│ 3. 运行日报分析脚本                                  │
│    cd wechat-article-skill                           │
│    node daily-analysis-llm.js                        │
│    → 默认时间范围：昨天7:00 到今天7:00               │
│    → 输出文件：output/{INDUSTRY_NAME}动态日报MMDD.md             │
│    → ⚠️ 注意：外部LLM API已禁用，改用AI Agent直接分析          │
│      新流程：node collect-articles.js 收集文章                  │
│      → AI Agent读取JSON提出候选核心观点                         │
│      → 对每个核心观点执行网络搜索验证                           │
│      → 只保留有来源支撑的观点，完成分析、去重、分类、生成日报   │
│    → 注意：脚本运行期间不要停止 exporter 服务         │
├──────────────────────────────────────────────────────┤
│ 4. 如脚本报 "invalid session"（Auth Key 过期）       │
│    → 访问 http://localhost:3200/api/public/v1/authkey │
│    → 将返回的 key 更新到 .env 文件的 AUTH_KEY 变量    │
│    → 重新运行脚本（步骤3）                            │
│    → 注意：Auth Key 有效期4天，通常不需要每次重新获取 │
└──────────────────────────────────────────────────────┘

阶段2：排版与保存草稿（需要 Chrome DevTools MCP）
┌──────────────────────────────────────────────────────┐
│ 5. 生成微信HTML                                      │
│    cd wechat-article-skill                           │
│    node markdown-to-wechat.js                        │
│      "../output/{INDUSTRY_NAME}动态日报MMDD.md"      │
│    → 输出：output/{INDUSTRY_NAME}动态日报MMDD.wechat.html│
│    → 同时生成 output/daily.html（ASCII别名，避免中文文件名编码问题）│
│    → 该HTML使用内联样式，微信编辑器直接兼容          │
├──────────────────────────────────────────────────────┤
│ 6. 在微信公众号后台编辑                              │
│    → 打开 https://mp.weixin.qq.com/                 │
│    → 如未登录，告诉用户登录后再继续                   │
│    → 点击"选择已有内容"                              │
│    → 系统会打开新标签页（含编辑器+选择面板）          │
│    → 用 evaluate_script 点击 .publish_list_item      │
│      选择最近的一篇文章（作为模板）                   │
│    → 点击"确定"加载文章到编辑器                      │
│    → ⚠️ a11y snapshot 无法捕获选择面板中的文章列表    │
│      必须用 evaluate_script 操作 .publish_list_item   │
│    → 如选择已有内容不可用，则点击"文章"创建新文章     │
├──────────────────────────────────────────────────────┤
│ 7. 修改标题                                          │
│    → 全选标题（Ctrl+A）→ 输入新标题                  │
│    → 标题格式（双轨标题）：                           │
│      {最劲爆新闻关键词}｜{M.D}数据中心、算力、AI日报                  │
│    → ⚠️ 标题中不要包含任何 emoji 或图标              │
│    → ⚠️ 日期必须是当天的日期，动态获取               │
│    → 逻辑：标题前半句必须来自当天最有公共关注度、     │
│      行业影响和搜索热度的新闻，但要写成"事件+影响/   │
│      悬念/冲突"，避免只堆关键词；用"｜"隔开，后面加日报标识 │
│    → 写标题前必须联网核对关键事实、热度和最新进展；   │
│      不得用夸大、惊悚、承诺收益或正文没有支撑的标题   │
│      例如：软银砸5900亿建欧洲最大AI算力中心，        │
│            英伟达杀入PC芯片市场｜6.2数据中心、算力、AI日报           │
├──────────────────────────────────────────────────────┤
│ 8. 替换正文内容                                      │
│    → 使用跨页面剪贴板粘贴方式（详见 SKILL.md）       │
│    → 在本地 HTTP 页面打开 daily.html                 │
│    → 复制 HTML 到剪贴板                              │
│    → 切回微信编辑器，清空正文并粘贴                  │
│    → ⚠️ 不要用 Ctrl+A 全选（会同时选中标题和正文）   │
│    → ⚠️ 不要用 insertHTML（会丢失所有样式）          │
├──────────────────────────────────────────────────────┤
│ 9. 保存为草稿                                        │
│    → 点击"保存为草稿"按钮                            │
│    → 等待保存成功提示                                │
│    → 告知用户草稿已保存，由用户自行审核后发布         │
│    → ⚠️ 不要点击"发表"按钮，仅保存草稿               │
├──────────────────────────────────────────────────────┤
│ 10. 清理（每次完成后必须执行）                       │
│    → 关闭本地 HTTP 服务器（端口 8766）               │
│      找到对应进程并终止                               │
│    → 删除过程文件（保留最终日报 .md 文件）：          │
│      - output/collected-articles-*.json               │
│      - output/*.wechat.html                           │
│      - output/daily.html                               │
│      - output/run_log.txt                             │
│      - output/test.txt                                │
│    → ⚠️ 不要删除 output/*.md 日报文件               │
└──────────────────────────────────────────────────────┘
```

## 关键技术细节

### Skills 索引

| Skill 名称 | 用途 | 调用时机 |
|-----------|------|---------|
| `wechat-editor` | 控制微信公众号后台编辑器（Chrome DevTools MCP） | 编辑/排版/保存草稿时 |
| `link-validator` | 审计日报链接合法性，识别 AI 幻觉链接 | 保存草稿出现"不合法链接"错误时 |

### Auth Key 管理
- 存储在 `wechat-article-skill/.env` 文件的 `AUTH_KEY` 变量中
- 有效期4天，通常不需要每次重新获取
- **仅在脚本报 "invalid session" 时**才需要重新获取
- 获取新 Key：访问 `http://localhost:3200/api/public/v1/authkey`
- 更新方式：修改 `.env` 文件中的 `AUTH_KEY` 值

### 时间字段
- `create_time`：文章创建时间（不准确）
- `sent_info.time`：实际发布时间（✓ 正确，代码已使用此字段）

### 花生编辑器（已弃用）
- ~~地址：https://editor.huasheng.ai/~~
- 现已改用 `markdown-to-wechat.js` 直接生成微信兼容HTML
- 正文粘贴方式：跨页面剪贴板粘贴（详见 `.trae/skills/wechat-editor/SKILL.md`）
- ⚠️ 微信后台是 HTTPS，无法从 HTTPS 页面 fetch 本地 HTTP 服务器内容（混合内容策略），必须使用跨页面剪贴板方式

### 微信公众号后台
- 地址：https://mp.weixin.qq.com/
- 流程：选择已有内容 → 选最近文章 → 确定加载 → 改标题 → 替换正文 → 保存草稿
- 标题格式（双轨标题）：`{最劲爆新闻关键词}｜{M.D}数据中心、算力、AI日报`（不含 emoji，M.D替换为实际日期如6.3，前半句踩热搜词，后半句保留日报标识）
- 正文从关注引导语开始（删除第一行主标题，因为标题已在标题栏填写）
- **仅保存为草稿，不要点击发表**，由用户自行审核后发布

### 日报内容结构（重要！）
日报采用"判断优先、快慢结合"模式，结构固定为：

1. **今日判断**（首屏留存）：先给一句当天核心判断，再列3个变化信号；这些判断是 Agent 对当日文章的归纳，但每一条都必须经过网络搜索验证，不得用过时模型记忆直接下结论。
2. **🔥 今日重点**（慢热点，做深）：最多3条，每条包含：
   - 新闻标题 + 原文链接
   - 详细摘要
   - **为什么重要**：1-2句话
   - **这意味着**：1-2句话，给从业者启示
   - **受益方**：点名最可能受益的公司/环节/岗位，不确定时写"待验证"
   - **承压方**：点名可能承压的环节/玩家，不确定时写"暂不明显"
   - **后续观察**：给一个可跟踪的下一步节点，例如签约、采购、投产、监管审批、财报披露
3. **⚡ 一分钟速览**（快资讯，做极简）：按分类（融资、建设、技术、风险、其他）用表格呈现
   - 列：动态概要 | 影响度（⭐1-5） | 原文链接
   - 每条新闻压缩为一行，不展开详细描述
   - 弱新闻宁可删除，不为凑数量保留会议稿、软文稿和重复转述
4. **关注引导语**（文末留存卡片）：一句话说明公众号价值，引导关注；不要放在首屏抢占判断信息
   - ⚠️ **不要写发刊时间**（如"每日 7:00 / 18:00 双发"），用户已明确要求删除时间承诺
   - 推荐写法：突出公众号价值 + 邀请关注的行动号召，不含具体时间

### 内容增长优化规则（重要！）

1. **Agent 生成的核心观点必须逐条网络搜索验证**：
   - 先由 Agent 基于收集到的文章提出候选核心观点，再对每个候选观点单独搜索验证；搜索不是可选步骤。
   - 必须验证的内容包括：标题前半句、今日判断、为什么重要、这意味着、受益方/承压方、后续观察。
   - 每个核心观点至少找到1个可靠来源支撑；涉及融资金额、政策、订单、投产、芯片/AI模型、公司战略等高时效内容，优先找2个来源交叉确认。
   - 优先使用官方公告、公司新闻稿、监管/交易所文件、权威媒体；如果只能找到二手消息，正文要降低语气，例如写"据报道/有待确认"，或直接删除该观点。
   - 如果搜索结果与候选观点冲突，以最新可靠来源为准；无法验证时不得写入标题、今日判断或重点分析。
   - 对AI、芯片、数据中心、能源、政策、融资金额等易变化信息，不得只凭模型记忆。
2. **标题要有打开理由，但不能标题党**：
   - 推荐结构：`事件关键词 + 行业影响/悬念/冲突`，例如"AI算力又要涨价？某云厂商加码GPU采购"。
   - 禁止结构：纯公司名堆砌、纯金额堆砌、"爆了/疯了/彻底变天"等正文无法支撑的夸张表达。
   - 标题长度优先控制在微信列表中可读的短句，前半句尽量不超过28个中文字符。
3. **首屏必须先交付判断**：
   - 打开正文后先看到"今日判断"，不要先看到关注引导。
   - 今日判断写成从业者能转发的结论，不写"今日有多条新闻值得关注"这类空话。
4. **重点新闻要写产业判断**：
   - 摘要只保留事实链条，不复述原文长段背景。
   - "为什么重要/这意味着"必须回答：谁受益、谁承压、下一步看什么。
5. **发布前补齐运营信息**：
   - 草稿标题使用双轨标题。
   - 摘要建议写成"今天最重要的一句话判断"，不要复述标题。
   - 封面图建议围绕当天主事件生成或选择，保持统一风格但避免纯装饰图。

**不要使用旧格式**（每个分类下用大段文字展开每条新闻），旧格式信息量过大，完读率低。

### 日报去重规则（重要！）

生成日报时**必须**与最近7天的日报进行去重，避免同一条新闻连续多天出现：

1. **读取 output 目录下最近7天的 .md 日报文件**
2. **提取已报道的新闻关键词**（标题中的核心事件/公司/项目名）
3. **去重逻辑**：
   - 如果今天收集到的新闻与最近7天已报道的新闻属于**同一事件**（如同一投资、同一政策、同一产品发布），则从今日日报中**移除**
   - 如果同一事件有**重大进展**（如从"计划"变为"签约"、从"传闻"变为"官方确认"），则**保留但标注更新**，例如：`（更新）软银750亿欧元法国AI数据中心项目正式签约`
   - 仅标题措辞不同但描述同一事件的，也算重复
4. **去重范围**：今日重点 + 一分钟速览，都要去重
5. **去重后如果新闻太少**：说明该时间段确实缺乏新动态，如实报道即可，不要为了凑数而保留旧闻

### 微信编辑器关键技术（重要！）
- **编辑器框架**：ProseMirror（富文本编辑器）
- **⚠️ 页面上有两个 `.ProseMirror` 元素**：
  1. 第一个 `.ProseMirror`（`querySelectorAll('.ProseMirror')[0]`）= **标题编辑器**（父元素 class: `title-editor__input`）
  2. 第二个 `.ProseMirror`（`querySelectorAll('.ProseMirror')[1]`）= **正文编辑区**（父元素 class: `rich_media_content`）
  - **绝对不要用 `document.querySelector('.ProseMirror')`，它返回的是标题编辑器！**
- **标题输入框**：`#title`（TEXTAREA 元素，与正文分离）
- **⚠️ 关键规则**：
  1. 标题和正文是**两个完全独立的元素**，不要混淆
  2. 设置标题：先操作标题 ProseMirror（`insertText`），再同步 `#title` textarea 的 value
  3. 设置正文：使用 `ClipboardEvent` 粘贴方式（保留排版），**不要用 `insertHTML`**（会丢失所有样式）
  4. **不要使用 Ctrl+A 全选**（会同时选中标题和正文，导致粘贴错位）
  5. **不要直接修改 innerHTML**（ProseMirror 不会注册变更）
  6. **必须先设置正文，再设置标题**（否则标题会被覆盖）
- **详细指南**：参见 `.trae/skills/wechat-editor/SKILL.md`

### Chrome DevTools MCP
- 阶段2（排版与保存草稿）必须通过浏览器操作，需要 Agent 具备 Chrome DevTools MCP 工具
- 如果 Agent 不支持浏览器操作，阶段1仍可独立完成（生成 Markdown 日报文件），但排版和保存草稿需手动操作
- **重要**：操作微信编辑器时，应调用 `wechat-editor` skill

## 常见问题

### Q: 运行脚本时报 "invalid session"
A: Auth Key 过期了。需要：
1. 浏览器访问 http://localhost:3200 扫码登录
2. 访问 http://localhost:3200/api/public/v1/authkey 获取新 Key
3. 更新 .env 文件中的 AUTH_KEY 值
4. 重新运行脚本

### Q: PowerShell 阻止 npx 运行
A: 使用完整路径：`& "C:\Program Files\nodejs\npx.cmd" nuxt dev --port 3200`

### Q: 脚本运行期间 exporter 服务停止了
A: 重新启动服务，然后重新运行脚本

### Q: 保存草稿提示"请勿输入不合法的消息图文链接"
A: 这是微信对正文中 `<a href="...">` 链接的校验失败。**核心解决方案：只过滤真正不合法的链接，保留所有合法链接**。

1. **找到 AI 幻觉的链接**（核心问题）
   - 跑 `_audit-links.cjs` 脚本，对比 `output/*.md` 中引用的 URL 与 `output/collected-articles-*.json` 中收集到的 URL
   - 不在 collected-articles.json 中的 URL = AI 幻觉出来的，原文根本不存在
   - 只把这类链接转成纯文字（如"原文（未收录）"），其他链接**全部保留**
2. **链接复用不是问题**
   - 同一 URL 被复用 10-20 次是正常的（汇总文章被拆出多条新闻）
   - 不要把"链接复用次数"作为过滤依据
   - RetKey:14 是 curl 自动化请求触发的风控验证码，**不代表**微信编辑器实际会拒
3. **修复流程**：
   - 跑 `_audit-links.cjs` 找出 3-5 个 AI 幻觉链接
   - 在 `.md` 中把对应行的链接改成"原文（未收录）"
   - 重新跑 `node markdown-to-wechat.js`（不传 `--whitelist` 参数）
   - 重新粘贴到微信编辑器

### Q: AI 分析生成了太多不重要的新闻
A: 大部分"日均 30+ 条新闻"是正常的。区分合理 vs 不合理：

1. **合理的多**：汇总文章（如"每日大事件"）里的多条独立新闻，每条都应保留
2. **不合理的多**：把同一具体事件在多篇文章里重复列出（应去重）
3. **优化 AI prompt**：
   - 显式说明"如遇汇总文章，按其中的独立新闻条目逐条记录，不要合并"
   - 但同一具体事件在多篇汇总文章里出现 → 只保留 1 条
4. **不要为了"少"而漏报**：宁可多列，不可漏掉真正重要的产业信号

### Q: markdown-to-wechat.js 生成 HTML 时把链接全删了
A: 之前误用了 `--whitelist` 参数的白名单机制：白名单外链接全部转成纯文字。**正确的做法是只过滤 AI 幻觉链接，保留所有合法链接**。

- 默认行为（不传 `--whitelist`）：所有链接都保留
- 如果某个链接触发了"不合法"错误 → 用 `_audit-links.cjs` 定位具体哪几个
- 手动在 `.md` 中把那 3-5 个 AI 幻觉链接改成纯文字
- 重新生成 HTML（不传 `--whitelist`），其他链接全部保留

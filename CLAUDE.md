# Claude Code 工作流配置

本文件是 AI 助手执行任务时的操作指引。项目介绍和配置说明见 [README.md](README.md)。

## 完整工作流程

当用户说"分析新闻"或"发日报"时，**直接按以下流程执行**，不需要询问是否使用网络搜索。

```
阶段1：生成日报
┌──────────────────────────────────────────────────────┐
│ 1. 检查 wechat-article-exporter 服务是否运行         │
│    → 访问 http://localhost:3200                      │
│    → 如未运行：启动服务                               │
│      cd "d:\Amateurinterests\微信公众号分析\wechat-article-exporter" │
│      npx nuxt dev --port 3200                        │
│    → 注意：PowerShell 可能阻止 npx，使用完整路径：    │
│      & "C:\Program Files\nodejs\npx.cmd" nuxt dev --port 3200 │
├──────────────────────────────────────────────────────┤
│ 2. 检查登录状态                                      │
│    → 访问 http://localhost:3200                      │
│    → 如显示"invalid session"或需要登录：              │
│      告诉用户"需要扫码登录，请在浏览器中操作"         │
│      等待用户扫码完成后再继续                         │
├──────────────────────────────────────────────────────┤
│ 3. 获取并更新 Auth Key                               │
│    → 访问 http://localhost:3200/api/public/v1/authkey │
│    → 将返回的 key 更新到 .env 文件的 AUTH_KEY 变量    │
│    → 注意：Auth Key 有效期4天，过期需重新登录获取     │
├──────────────────────────────────────────────────────┤
│ 4. 运行日报分析脚本                                  │
│    cd "d:\Amateurinterests\微信公众号分析\wechat-article-skill" │
│    node daily-analysis-llm.js                        │
│    → 默认时间范围：昨天7:00 到今天7:00               │
│    → 输出文件：output/{INDUSTRY_NAME}动态日报MMDD.md │
│    → 注意：脚本运行期间不要停止 exporter 服务         │
└──────────────────────────────────────────────────────┘

阶段2：排版与发布
┌──────────────────────────────────────────────────────┐
│ 5. 在花生编辑器中排版                                │
│    → 打开 https://editor.huasheng.ai/               │
│    → 将 output/ 中的 Markdown 内容粘贴到编辑器       │
│    → ⚠️ 删除第一行标题（# {INDUSTRY_NAME}动态日报|日期）│
│      只保留从"## 🔥 今日重点（AI筛选）"开始的内容    │
│      因为标题在微信编辑器中单独填写，正文里不需要重复 │
│    → 点击"复制到公众号"按钮                          │
├──────────────────────────────────────────────────────┤
│ 6. 在微信公众号后台编辑                              │
│    → 打开 https://mp.weixin.qq.com/                 │
│    → 如未登录，告诉用户登录后再继续                   │
│    → 点击"选择已有内容"                              │
│    → 选择最近的一篇文章（作为模板）                   │
│    → 点击"确定"加载文章到编辑器                      │
├──────────────────────────────────────────────────────┤
│ 7. 修改标题                                          │
│    → 全选标题（Ctrl+A）→ 输入新标题                  │
│    → 标题格式：{INDUSTRY_NAME}动态日报 YYYY年M月D号   │
│    → ⚠️ 日期必须是当天的日期，动态获取               │
│      INDUSTRY_NAME 从 .env 文件读取                  │
│      例如：INDUSTRY_NAME=算力，数据中心，AI           │
│      → 算力，数据中心，AI动态日报 2026年5月31号       │
├──────────────────────────────────────────────────────┤
│ 8. 替换正文内容                                      │
│    → 点击正文编辑区域                                │
│    → Ctrl+A 全选 → Delete 删除旧内容                 │
│    → Ctrl+V 粘贴从花生编辑器复制的新内容             │
├──────────────────────────────────────────────────────┤
│ 9. 保存为草稿                                        │
│    → 点击"保存为草稿"按钮                            │
│    → 等待保存成功提示                                │
│    → 告知用户草稿已保存，由用户自行审核发布           │
└──────────────────────────────────────────────────────┘
```

## 关键技术细节

### Auth Key 管理
- 存储在 `.env` 文件的 `AUTH_KEY` 变量中
- 有效期4天，过期后 API 返回 `invalid session`
- 获取新 Key：访问 `http://localhost:3200/api/public/v1/authkey`
- 更新方式：修改 `.env` 文件中的 `AUTH_KEY` 值

### 时间字段
- `create_time`：文章创建时间（不准确）
- `sent_info.time`：实际发布时间（✓ 正确，代码已使用此字段）

### 花生编辑器
- 地址：https://editor.huasheng.ai/
- 操作：粘贴 Markdown → 删除第一行标题 → 点击"复制到公众号" → 在微信编辑器中 Ctrl+V

### 微信公众号后台
- 地址：https://mp.weixin.qq.com/
- 发布流程：选择已有内容 → 选最近文章 → 确定加载 → 改标题 → 替换正文 → 保存草稿
- 标题格式：{INDUSTRY_NAME}动态日报 YYYY年M月D号（INDUSTRY_NAME 从 .env 读取，日期取当天）
- 正文从"今日重点"开始（删除第一行主标题，因为标题已在标题栏填写）

## 常见问题

### Q: 运行脚本时报 "invalid session"
A: Auth Key 过期了。需要：
1. 浏览器访问 http://localhost:3200 扫码登录
2. 访问 http://localhost:3200/api/public/v1/authkey 获取新 Key
3. 更新 .env 文件中的 AUTH_KEY 值
4. 重新运行脚本

### Q: PowerShell 阻止 npx 运行
A: 使用完整路径：`& "C:\Program Files\nodejs\npx.cmd" nuxt dev --port 3200`

### Q: 脚本运行期间 exporter 服务停止了
A: 重新启动服务，然后重新运行脚本

### Q: 在线服务 down.mptext.top 不可用
A: 正常，该服务不稳定。使用本地服务替代。
